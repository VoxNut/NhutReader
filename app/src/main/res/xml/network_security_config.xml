<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- AnkiConnect can run on user-provided private HTTP hosts; app code rejects public HTTP URLs. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
